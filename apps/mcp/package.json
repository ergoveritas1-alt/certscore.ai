{
  "name": "@certscore/mcp-http",
  "version": "0.2.12",
  "private": true,
  "description": "Remote Streamable HTTP MCP service for CertScore.",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "pnpm run build:deps && tsc -p tsconfig.json",
    "build:deps": "pnpm --filter @certscore/api-contracts build && pnpm --filter @certscore/sdk build && pnpm --filter @certscore/mcp-auth build && pnpm --filter @certscore/mcp build",
    "dev": "pnpm run build:deps && node --env-file=../web/.env.local --enable-source-maps --import tsx src/index.ts",
    "start": "node --enable-source-maps ./dist/index.js",
    "typecheck": "pnpm run build:deps && tsc --noEmit -p tsconfig.json",
    "test": "node --import tsx --test src/*.test.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@certscore/api-contracts": "workspace:*",
    "@certscore/mcp-auth": "workspace:*",
    "@certscore/sdk": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@certscore/mcp": "workspace:*",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^24.0.0"
  }
}
