# Saved site report — scanso2

Presentation snapshot saved September 11, 2026.

Route: /app/scanso2/[scanId]

This directory preserves the report components from components/scans at save time.
Edit components/scans for ongoing report development; leave this snapshot unchanged
unless intentionally updating the saved version.

Authentication, canonical evidence/projection libraries, backend APIs, shared
non-report components and global styles remain shared. Scan data is not duplicated.
This is a saved presentation version, not an immutable data or full-application archive.
