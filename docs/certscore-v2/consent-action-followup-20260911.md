# Accept/Reject follow-up validation — September 11, 2026

The confirmed early-exit defect is fixed. Fresh Ireland pairs complete all five targeted paths on the changed build versus three on deployed baseline. The earlier Blick Accept and Mackolik Accept/Reject losses did not recur. These checks support proceeding to the canonical AWS release when requested; no deployment occurred. They do not establish a production completion rate or prove the exact cause of the earlier live losses.

## Fix and evidence boundaries

A named CMP selector can resolve before its live label becomes classifiable. Final action proof correctly rejects that weak label, but previously ended discovery even with original search time remaining. Accept and Reject now allow one canonical rediscovery for the two explicit weak/unclassified-label reasons within the original deadline, followed by fresh confirmation baselining and full final proof. Opposite decisions, semantic conflicts, transactional controls, duplicate targets, cancellation and expired authorization remain guarded. There is still at most one click.

The shared proof records bounded labels already read, their source names and selector/frame hashes in the existing 12-snapshot operational diagnostic cap. This adds no DOM read and cannot establish dispatch, consent registration, a finding or a score effect. No lane, navigation, model call, outer deadline, action tail cap or observation window was added. Earlier canonical multilingual semantic and final-binding fixes remain part of the tested build.

## Deterministic verification

Source-configured fast preflight recorded 2,098 test executions with zero failures, including overlapping suites, and completed its typecheck phases. The aggregate shell session was lost on a user-message interruption; its final database typecheck was separately rerun successfully. Thirteen focused source-configured cases passed. The deployed-baseline late-label fixture fails both action paths as expected; current code recovers both. Negative fixtures cover opposite/conflicting labels, duplicate controls, cancellation and labels arriving after the deadline.

A separate literal hidden OneTrust fixture behaves identically on baseline/current: visibility inside the original 2.5-second budget permits a click; visibility after the budget does not. This supports preserving the deadline rather than extending it for Blick. Initial fixture runs that omitted the explicit source tsconfig could load stale compiled contracts and are excluded; `deterministic-validation.json` identifies authoritative and superseded logs.

## Paired localhost results through Ireland

Six normal full six-lane scans contacted three eligible domains on September 11, 18:59–19:02 UTC. Browser and HTTP/TLS egress were verified as `63.33.9.201` in AWS `eu-west-1`; TLS verification remained enabled. Baseline was deployed revision `6f704ff2d4f226165d130b40479d2f039fbb379d`. Source hashes were verified for both checkouts. Order alternated: baseline first for Blick/Mackolik, current first for PestPac. ErgoVeritas was excluded, the existing cooldown waiver applied, and no blocked/manual-hold target, no-go retry or extra target retry was used.

| Targeted path | Baseline | Changed build |
| --- | --- | --- |
| Blick Accept | Click; registered | Click; registered |
| Mackolik Accept | Click; complete after-click capture | Click; complete after-click capture |
| Mackolik Reject | No click | Click; complete after-click capture |
| PestPac Accept | Click; complete after-click capture | Click; complete after-click capture |
| PestPac Reject | No click | Click; complete after-click capture |
| Total targeted completed clicks | 3/5 | 5/5 |

The five paths were the fixed diagnostic targets. Across all six scheduled A/R slots per variant, clicks were 3/6 versus 5/6. Blick Reject did not click in either variant: baseline retained a no-control outcome; current retained an explicit passive-barrier coverage limitation, with no retained Reject action packet. Its independent control absence was not established. Current Blick first-layer Reject inventory also remains unknown. Neither that slot nor the limited inventory is a success or evidence of absence.

All six scans passed retained-pointer/hash, typed packet where present, canonical persisted action-projection, single-terminal-result and local API checks. That verifies correct preservation of successes and limitations, not success of every lane. Current Blick registered Accept. The four Mackolik/PestPac clicks retained complete after-click observations with unconfirmed registration; those observed facts remain useful and are not relabeled as registered consent/refusal. No post-action drops occurred. All completed unconfirmed captures ended at their configured window.

Every current live click had `reResolvedBeforeDispatch: false`: the newly added weak-label rediscovery branch was proven in fixtures but was not exercised by these live successes. Do not attribute the live improvement specifically to that branch. Dynamic discovery remains variable: PestPac baseline Accept succeeded this time, whereas both baseline paths had failed in the prior pass. The selected three-domain sample is too small and nonrepresentative for a production error-rate estimate. Preserve the [earlier 19-scan report](consent-action-ireland-validation-20260911.md) as historical evidence.

## Latency and cost

| Domain | Baseline scanner time | Changed scanner time | Difference |
| --- | ---: | ---: | ---: |
| Blick | 21.369 s | 17.016 s | −4.353 s |
| Mackolik | 20.567 s | 18.240 s | −2.327 s |
| PestPac | 13.413 s | 17.917 s | +4.504 s |

Recovered capture can use more of the existing time allowance. PestPac's current Reject lane added 5.903 seconds beyond the passive barrier, within the unchanged eight-second cap, versus no Reject tail in baseline. Its overall scan was 4.504 seconds longer. The paired median was −2.327 seconds, but three pairs cannot establish production latency improvement or p95 equivalence. Evidence capture was not shortened to improve timing.

Logged normal validation model cost was $0.0070845. The disclosed one-time allowance including existing proxy/contact diagnostics was below $0.10. Added bounded proof metadata is estimated below $0.10/month at 100,000 scans and 30-day retention, within the prior $5/month action-fix approval. The earlier successful-dispatch compute planning allowance remains up to $2/month; this validation is not a production cost measurement. No new infrastructure was provisioned.

## Operational record and disposition

All six actual contacts were persisted with idempotent key `action-path-followup-20260911-paired-ie-v1`. The reviewed per-run ledger contains three cooldown entries and no no-go outcomes; it does not replace the canonical 51-target ledger. Fresh central export verifies the contacts. The tunnel was stopped, its exact temporary SSH rule revoked, and both rule/listener absence independently checked.

No demonstrated targeted click regression remains in this follow-up. Keep the explicitly limited Blick Reject outcome and the earlier losses visible; do not claim universal coverage. The next useful step is the canonical AWS release followed by a representative production cohort comparison of click completion, after-click coverage, wrong-click guards and p95 latency. No further deadline extension or evidence relaxation is recommended from this sample.

Implementation remains reviewable in the working tree. This report and the [per-run contact ledger](calibration-runs/2026-09-11-action-path-followup/README.md) are bookkeeping, not a deployed-release claim.

Artifacts: `artifacts/action-path-followup-20260911/`, especially `deterministic-validation.json`, `preflight-final.log`, source-configured fixtures, and `live/analysis.json`, per-scan verifications/source hashes, egress proofs, contact accounting and `cleanup-verification.json`.
