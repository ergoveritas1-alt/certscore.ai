# Cookie/tracker graph release record

Execution authorized by product owner on 2026-09-04: implement the preceding plan, including its $25/month and $15 release/test ceilings.

## Baseline and cost gate

- Baseline checkout and live web: `aa8750dfa5110331f89ba3d3f371cdd8f76a9222`.
- All three scanner regions: `sha256:ad97a614242a0c787182bae7af840af972881d80db00d2536215a88dd386c08f`, healthy, x86_64, 3,008 MB.
- AWS/GitHub access verified. Node 22.22.1 and pnpm 10.6.3 verified.
- Approved read-only ECS database aggregate: 26,874 scans in the preceding 30 days. This includes all scans, conservatively more than graph-eligible scans.
- Live S3 policies in all three regions retain current objects; the 90-day expiration applies to noncurrent versions only.
- The initial $23.92/month planning estimate is **superseded**: it did not account adequately for full graph projections inside the existing database. The production RDS allocation is 20 GiB; CloudWatch minimum free space in the audited interval was 7,241,641,984 bytes (about 6.74 GiB). Do not put full graphs in report JSONB or increase provisioned capacity.
- Reference-based persistence now retains one public-safe, content-addressed projection in the existing private shared bucket and only a bounded summary/reference in the report. Its bucket is `ws01-scan-artifacts-199536052647-us-west-1`; public access is blocked and no lifecycle expiration is configured. Existing live web/materializer task-role GetObject/PutObject permission was verified with IAM policy simulation. No new permission or bucket is required.
- Retained-byte ceiling: four runtime-bearing graphs × 128 KiB × two durable raw representations (lane-owned packet/bundle and merged canonical bundle) = 1 MiB, plus one public graph artifact capped at 768 KiB = **1.75 MiB per scan**. Lambda writes compact canonical/action JSON. WC01's local mirrors are ephemeral files, not additional indefinitely retained S3 copies. Canonical publication uses content-addressed conditional writes; repeated valid compact publication preserves the reference without another write. Tests ensure graph bytes never displace existing evidence during compaction.
- At 26,874 scans/month, this ceiling adds about 45.93 GiB/month and 551.12 GiB by month twelve. AWS Pricing standard-storage rates: Frankfurt $0.0245, Ireland $0.023, California $0.026 per GiB-month. Applying the highest rate throughout gives approximately **$14.33/month storage at month twelve**. Add planned 250 ms worker overhead across four lanes (~$1.32/month), $0.70 coordinator/projection/logging reserve and $3 requests/transfer reserve: **$19.35/month**, inside the approved $25 ceiling. A ≤3 KiB database reference/summary adds <1 GiB/year at this volume, inside the existing allocation; no capacity increase is authorized or planned.
- This forecast is a conservative planning bound, not a measured production bill. Recalculate measured duration, retry/version rates, artifact copies, reference bytes and requests before each promotion. Hold rollout if it no longer fits. The approved ECS read-only audit is charged to the $15 release budget (<$0.01 estimated); no deployment, model call or live release scan has yet been charged.
- At full rollout, volume growth or retention beyond twelve months requires a renewed forecast. The feature rollout budget is not an instantaneous AWS billing cap.

## Progress

- [x] Production topology, access, and initial cost gate checked.
- [x] Contracts and deterministic graph tests.
- [x] Browser capture and attribution corrections (focused gates passed; full release regression pending).
- [x] Lane binding, persistence, API, and UI (local implementation; deployed round trip pending).
- [ ] Fixture/replay/browser/performance gates.
- [ ] Feature-off code deployment and regional verification.
- [ ] Canary and staged customer activation.
- [ ] Post-deployment observation and handoff.

No implementation, live canary, deployment, or performance gate is recorded as passing until its evidence is retained here.

## Local implementation and verification log

Working branch: `codex/cookie-tracker-evidence-graph`. Changes remain uncommitted; production remains on the recorded baseline with graph activation off. No public or owned live scan has been created for this release yet.

- Implemented versioned bounded graph capture, protocol identities/redirect correlation, HTTP/JS cookie distinctions, storage/worker/frame metadata, passive streaming counters, scenario/action anchors, per-capture keyed value digests, verified lane merge, persisted public-safe projection, API schema/OpenAPI, and the inventory relationship explorer. Existing scoring policy is unchanged.
- Re-ran the contracts suite: **317/317 passed**, including unsupported/malformed optional graph compatibility and action-packet isolation. Vendor resolver: **137/137 passed**. API contracts/OpenAPI: **16/16 passed**. SDK build passed.
- Focused graph correlator plus real Chromium fixtures: **17/17 passed**. Includes full runtime-lane integration, shuffled ExtraInfo, duplicate URLs/frames, stylesheet/font source chains, blocked/cached/worker-served facts, lifetime/deletion semantics, host-only/domain cookie collisions, graph limits/freeze, malformed references, and page tampering/native-equivalence tests.
- Verified retained projection tests: **7/7 passed**. Checks hash/source/scan ownership, missing action packets, privacy leakage, historical unavailability, and retained policy text ownership/hash/coverage. Storage/read/API round trip: **51/51 passed**; authorization plus projection: **10/10 passed**. Full graph bytes do not enter database report payloads. Corruption, cross-scan, count drift, upload failure, compact re-publication, missing permissions, kill switch and old scans fail closed without score/finding changes.
- Dedicated coordinator identity/mode/hash/diagnostic test: **1/1 passed**. Server-owned rollout/public-override test: **1/1 passed**.
- Web, scanner-core, and Lambda focused typechecks passed before subsequent test/documentation additions; final exact-source checks remain required.
- Lambda suite: **109/109 passed**, including actual production esbuild/minified probe execution in Chromium. Review engine: **79/79 passed**; report adapter: **258/258 passed**. MCP package: **86/86 passed**, including graph identity/edge preservation; hosted MCP quota: **8/8 passed**, with matching canonical evidence weights for scan/domain inventory reads. Final exact-source regressions remain required after subsequent changes.
- Local app UI verified through the permitted browser on `http://localhost:3000/dev-fixtures/runtime-evidence-graph`: expansion, multiple parents, navigation, pagination, search, scenario selection, keyboard Enter activation, evidence drawer, historical unavailable state, visual layout and empty error/warning console. This is clearly labelled synthetic development-only data; production returns not-found. One in-app renderer crashed during development/HMR; a fresh tab successfully completed checks. This is not a completed scan-selection/API end-to-end test.
- Astra independently reproduced and reviewed inherited `toJSON` nonce leakage, poisoned string/regex methods, custom Error stack formatting, missing action-packet gating, overlong cookie scope, cookie-domain collisions, and deletion corroboration. Fixes and committed-source regression tests were added. Astra confirmed the serialization/native/regex/long-path reproductions were closed; the collision/deletion regressions pass locally.
- The 100-pair randomized local fixture benchmark retained in `runtime-graph-local-benchmark-2026-09-04.json` preserved paired network/script/frame/HTTP-cookie counts. All-workload p95: 1,870.53 → 1,884.94 ms (+0.770%; 95% bootstrap ratio interval 1.00131–1.01298). Heavy workload passed, but light-workload uncertainty remained **inconclusive** (upper ratio 1.20199), so the overall release performance gate is **not passed**. Max raw graph 120,785 bytes; setup p95 ~2.02 ms, finalization ~1.28 ms. A 200-pair final-source run is required, without simultaneous heavy tests. Baseline is the same candidate checkout with capture disabled; this is not regional Lambda/barrier/handoff measurement.
- Deferred UI smoke passed on localhost:3000 with synthetic API interception: no reads while collapsed, one read reused across searches/scenarios/details, keyboard expansion, multiple parents, scoped scenario comparison, evidence drawer, 429 without automatic retries, explicit retry and cross-scan rejection. No page errors. Server storage/access/quota tests cover the actual read boundary separately; fresh production round trip is still pending.
- Full scan-core run stopped after 374 successful assertions because an owned Chromium process leaked on an existing tiny/combined scan path. A focused regression reproduced the failure with the original source and passed after requiring policy scanning to be enabled before retaining its recovery browser. This is a cleanup correction, not a runtime/browser/coverage change. The interrupted suite is not counted as passed; the full suite is running again. Root/action compaction plus cleanup tests: **7/7 passed**.
- That full rerun completed with **909 passed, 3 failed, 35 skipped**; it is not a release pass. A new full run retains TAP and JUnit in `/tmp/certscore-graph-release-tests-GRxneP/` so every failure remains inspectable. One failure was test-owned directory cleanup racing the intentional transport soft deadline; bounded cleanup retries preserve its assertions. Another was the GPC fixture scheduling its late request from navigation, before transport work and potentially before the quiet gate; the fixture now schedules from the existing retained-transport checkpoint immediately preceding the gate. Its original ≥400 ms restart assertion is preserved. Focused GPC/transport tests: **10/10 passed**. The remainder of the full suite is still under review.
- Further production-path review identified the actual durable FIFO producer in `apps/validation-worker`, beyond the web dispatch helper. Full and eligible sharded preview creation now allocate their UUID server-side and commit the server-owned graph selection (including explicit disabled/null) in the same scan/outbox insert. The publisher validates that persisted scan-bound decision and never reevaluates rollout on retry. Supplied client decisions are overwritten, historical/malformed/cross-scan decisions stay disabled, and consent-action settings remain independent. Focused dispatch contracts: **26/26 passed** before subsequent exact-target/preview additions; dispatch/canary/rollout contracts after those additions: **12/12 passed**. This requires the canonical validation-worker deployment in addition to web/materializer, Lambda and MCP.
- Registered owned canaries can be selected at zero customer percentage using a bounded deployment-only exact-URL list, checked against the existing owned registries by the rollout helper. No host/prefix/query/redirect matching, scan creation, or consent-action authorization is introduced. The profile is frozen at creation; clearing capture/cohort/canary settings affects **new scans**, not queued or retrying scans. `CERTSCORE_RUNTIME_GRAPH_PRESENTATION=off` independently suppresses graph exposure on subsequent canonical reads without deleting retained evidence.
- Rollout plan-only validation inspected the live web/materializer and made **no AWS mutation**. Activation now additionally requires the tested validation-worker, MCP (for project mode), and immutable scanner images in all three regions. Existing validation and MCP images were `315bb47a7904943b2b085444a1362d3d3040cd23`; they must be updated before graph activation. Validation task `certscore-validation-worker:455` is 256 CPU/512 MiB, one task; no capacity change is planned.
- Other completed boundary checks: retrieval/projection/documentation **57/57**, focused release contracts **76/76**, dispatch/result-poller/API/shared-report **164/164**. Final exact-source preflight remains required after the subsequent production-dispatch work. Deferred UI harness is `scripts/runtime-graph-ui-smoke.ts`.
- Latest production-dispatch/preview/rollout checks: **23/23 passed**. Updated combined graph release check: **88/88 passed**. Contracts: **318/318 passed**. API contracts/OpenAPI: **16/16 passed**. MCP dependency builds passed; SDK **25/25**, MCP auth **4/4**, MCP package **86/86**, hosted MCP **33/33** passed. Web, validation-worker, scanner-core and hosted MCP typechecks passed. The moved local UI harness passed again with four synthetic reads and no page errors. These results do not substitute for production round-trip or performance gates.
- Astra's additional read-only review confirmed the persisted dispatch choice and exact owned-target canary design; the identified preview-creation bypass was fixed using the same binder, and activation now checks every independently deployed participant. An unused legacy disclosure fallback was removed so graph policy mentions have only the checksum-verified retained-text path; projection/storage/rollout rechecks **15/15 passed**.
- The full suite's 35 skipped tests are the explicitly disabled general consent-flow research suite (`consentFlowResearchTest = test.skip`), not missing dependencies. The separately authorized bounded Accept/Reject lanes remain covered by their active suites. This release does not enable the general research flow.
- The retained full rerun finished **910 passed, 2 failed, 35 intentionally skipped**, not a release pass. Its GPC failure used the old timer fixture already repaired above. The other failure was the unchanged generic-negative representative screenshot fixture: the baseline's 450 ms shared capture deadline subtracted a 750 ms fallback reserve, starving CDP with 1 ms. The isolated original fixture reproduced the failure. The corrected budget partitions the same 450 ms into 225 ms CDP / 225 ms reserved Playwright fallback and preserves larger-profile budgets, both methods, document binding, failure diagnostics and classification. The original real-browser fixture passes; a new forced-stall test confirms the real Playwright fallback retains same-document PNG evidence within the existing 950 ms allowance. The budget unit passes, and scanner-core typecheck passes. This improves the short-profile allocation but leaves less fallback time after genuinely stalled CDP than the original allocation; it is not a universal latency claim.
- Full preflight now explicitly includes graph/browser/retention/deadline regressions and both representative viewport tests, which the existing late-consent name filter did not cover. Exact-source full preflight and final benchmark are still pending.
- Astra's final activation review found that reader readiness must be checked whenever presentation is enabled, even with new capture `off` or `capture_only`: previously retained/queued project graphs can still be displayed. The helper now independently gates producers by capture mode and MCP readers by presentation mode, with all six combinations covered by a new regression. Full suppression (`off` + presentation `off`) remains available without healthy independently deployed participants. Zero percent with exact owned URLs restricts **new capture**, not historical display; presentation is global and queued profiles are immutable.
- A final default-off audit corrected presentation's prior implicit-on behavior. Only exact `CERTSCORE_RUNTIME_GRAPH_PRESENTATION=on` permits exposure/hydration; missing, empty, invalid and `off` settings suppress copies without changing retained evidence. The explorer renders nothing for an undefined (disabled/absent) projection, while explicit typed `unavailable` projections keep their neutral explanation. The dev/browser harness covers both distinct states. Astra confirmed this preserves optional historical-scan semantics; these final web changes await their exact-source rerun and do not alter measured browser-capture source.
- Final-source local benchmark **passed**: `runtime-graph-local-benchmark-final-2026-09-04.json`, 200 randomized pairs (100 light / 100 heavy), fresh browser contexts and unchanged observation settings. Paired network/script/frame/HTTP-cookie counts matched in every pair. All-workload p95 1,858.714 → 1,889.773 ms, **+31.059 ms / +1.671%**, 95% bootstrap ratio interval **[1.00866, 1.01972]**. Light +0.617% (upper ratio 1.01426); heavy +1.405% (upper ratio 1.02589). Every workload passes both the p95 ≤1.05 ratio and upper-interval ≤1.05 gate. Max raw graph 120,787 bytes; setup p95 2.096 ms; finalization p95 1.064 ms. Full measured-source hashes are retained. No competing builds or browser suites ran; bounded read-only audit and web-only edits occurred. These are local scanner timings, not Lambda/barrier/upload/WC01 handoff measurements.
- Final presentation/storage/API/activation focused checks: **60/60 passed**, including explicit opt-in and all six capture/presentation readiness combinations.
- Updated local UI smoke passed: disabled input renders no tile, explicit historical unavailability makes no read, and all previous deferred graph/keyboard/scenario/429/privacy checks pass (four synthetic reads, no browser errors). Full preflight passed **89 release contracts, 39 capture/deadline regressions, both representative screenshots and deployment topology**, then caught a CommonJS-incompatible `import.meta` in the new contracts fixture test. Replaced only that fixture-path expression with its CommonJS-compatible `__dirname` equivalent; focused contracts **4/4** and all **19 workspace typechecks** now pass. Full preflight is rerunning; the interrupted attempt is not a full pass.
- The second full preflight passed the same early gates and all typechecks, then its local web build exhausted the configured 6 GiB Node heap (18/19 workspace builds completed). An isolated web build with command-scoped `NODE_OPTIONS=--max-old-space-size=8192` compiled, generated all 193 pages and completed traces successfully. No source, runner, AWS capacity, ECS memory or production configuration was changed by this local-only retry; incremental infrastructure spend is $0. Full preflight is rerunning with that explicit local heap allowance. The unchanged AWS image workflow must still pass its own build gate before promotion; local success does not establish CI memory sufficiency. Build-time runtime-environment checks skipped live DB/S3 validation as configured by `--allow-missing-env`; that is not a live runtime pass.

Latest exact-source release check:

- The third full preflight passed all 19 workspace builds/typechecks and all 250 validation-pipeline tests, then failed one of 32 late-consent fixtures. Repeated warm runs reproduced the failure; an isolated cold pass was not accepted as a gate. Diagnostics in `/tmp/certscore-graph-final-gates-6VQMEU/late-consent-control-timeline.log` show the last control observation at 1,148 ms, before the fixture's original 1,100 ms post-navigation insertion could occur (browser/context/probe setup alone exceeded 85 ms). The banner was retained; the failed assertion was missing Accept, not banner presence. Capture was disabled in this fixture.
- The fixture now has an explicit test-only deferred mode. The test asserts no buttons at the actual recapture-wait boundary, schedules insertion 100 ms into that native bounded wait, and preserves every original A/R/O, label, recapture, visual-proof and completion assertion. Default corpus HTML retains its original 1,100 ms timer. Astra reviewed this synchronization; it tests controls arriving after the initial empty inventory, not guaranteed detection of arbitrary 1,100 ms delayed controls. Focused warm pair **2/2 passed** and scanner-core typecheck passed. No production timing or acceptance label changed.
- Added the repository-required `server-only` guard to the S3 persistence module; storage and API round-trip tests use the existing server-module test isolation pattern and pass. Other new graph helpers are pure dependency-injected/typed transformations, without credential or I/O access.
- Exact-source full preflight is running again, followed sequentially by the complete scan-core suite. No deployment or activation has occurred. The final local capture benchmark remains source-valid because none of its ten measured runtime files changed.
- The fourth full preflight **passed** with the command-scoped local 8 GiB heap allowance: 89 graph release contracts, 39 capture/deadline regressions, 2 representative screenshots, topology, all 19 workspace typechecks/builds, 250 validation-pipeline tests, 32 consent-quality fixtures, 194 policy tests, 109 Lambda tests, 446 canonical-parity tests and the complete post-refusal release gate. Log: `/tmp/certscore-graph-final-gates-6VQMEU/preflight-4.log`. The complete scan-core suite started sequentially after that successful exit; its result is still pending. The unchanged AWS image build must independently pass before promotion.

### Explicit coverage limits

- No added browser lane, model call, live DNS lookup, longer observation window, raw value/body retention, historical backfill, or scoring change.
- Page-realm storage/IndexedDB/Cache metadata is not browser-verified storage success; it is marked `page_realm_snapshot` and `native_call_returned`. Direct property writes and record-level database/cache writers remain unsupported. Cookie Store dictionary overload identity is unresolved rather than executing additional getters/Proxy traps.
- Related workers/OOPIFs are attached without pausing them. Earliest activity and unrelated service-worker targets are not guaranteed; those limits are retained. Deep asynchronous stack references are not expanded.
- Scenario matching compares scoped identities only (including host/domain and partition distinctions), not values, persistence, absence, active use or consent effectiveness.
- Policy matching is deterministic literal product/vendor/entity mention in verified retained owned documents, not a legal sufficiency conclusion. Inadequate policy coverage remains unknown.

### Open release gates

Full regression/preflight, deployed performance gates, AWS deployments, regional canaries, ledger-selected public calibration, staged activation and 24-hour observation are **not yet complete**. The final local performance gate passed as recorded above. Existing canonical finding/score policy remains unchanged. No calibration labels or acceptance baselines have changed.

Two stale source assertions were verified against `git show HEAD` and corrected to check the existing behavior: explicit three-line collapsed/unclamped expanded report styles and current transport copy; Pulse status's parallel report/status lookup after quota denial handling (the previous assertion searched for a now-absent direct `await getPublicScanRecord`). No report behavior or rate limits were weakened. A new assertion checks graph endpoints explicitly request the canonical evidence weight.

### Staged-review sample definition (before live outcomes)

Use stable 5/25/100 scan-level cohorts. At each stage, review the first ten normally initiated graph-enabled completed scans, retain incomplete/no-go outcomes separately, and require zero critical privacy/integrity/canonical-finding regressions. Record every limitation, coverage denominator and score delta; absence of traffic is not a pass. Cover all three scanner regions with owned canaries at each stage. Keep regional latency claims descriptive if sample uncertainty is insufficient. Reserve at most 120 scans for initial paired owned-canary measurements (20 off/on pairs per region); the remaining approved owned-scan budget covers activation checks and bounded repair verification. The global 240-owned/10-public/$15 ceilings remain authoritative. Only independently authorized action-canary targets may exercise Accept/Reject; passive owned fixtures never gain interaction permission.
