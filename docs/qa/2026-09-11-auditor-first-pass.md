# Auditor first pass

Implemented through the shared canonical pipeline; no target-specific rule or display-layer finding was added.

- Generic privacy-contact navigation is rejected in the shared contracts classifier and Article 13 evidence validation. Historical accepted signals are revalidated during normalized concern construction. Unsupported evidence remains unconfirmed and does not become an absence finding.
- Retained HTML headings are selected only from uniquely matching exact supporting passages. Generated classifier headings are not presented as document headings. Captured titles, actual document headings, source URLs, and retained retrieval times remain separate.
- Independent Reject coverage limitations remain neutral and are distinguished from initial control inspection. Compact control cards show their explicit retained statuses.
- Finding-specific remediation is generated by the shared concern-policy guidance, persisted in the canonical evidence projection, and consumed by findings and report details. JSON-first evidence presentation is preserved.
- Report projection writer advances to v26 while v25 remains readable; deterministic local materialization and priority caches are versioned.

Verified the local scan `7cd83339-8253-48e0-9b9a-6a439db10863` by rebuilding through the canonical publisher from its retained evidence. Controller/contact disclosure is Not confirmed. Page score remains 56 with three priority findings. Source title and actual Privacy Policy heading are separately visible. JSON evidence, explicit initial control states, independent Reject coverage, and targeted retest instructions remain available.

Validation: 467 focused boundary tests passed; shared guidance/top-finding tests and compact Reject-card tests passed. Contracts build, web TypeScript, and whitespace checks passed. Browser verification checks the canonical result and hydration/runtime errors.

Broader suites exposed three failures outside the changed logic: legacy Reject-finding filtering in the regulatory lens, a HubSpot CMP naming expectation, and a stale vendor-resolver version expectation in materialization tests. These are not included in the focused passing claim.

No new scan, model call, lane, retry policy, or timeout increase. Bounded retained document-heading metadata is estimated below $1/month at 100,000 scans/month; disclosed during implementation. No deployment performed.
