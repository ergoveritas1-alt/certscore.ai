# Scoring redesign goal completion audit

Status: **incomplete; goal remains active**. Last audited 2026-07-22 against the
current repository and production revision `2473cae1`.

This audit separates proved implementation from remaining cutover work. A green test
or deployed shadow endpoint is not evidence that the replacement has been approved or
made customer-facing.

| Goal requirement | Current evidence | Status |
| --- | --- | --- |
| Stop report/admin reads from overwriting scores | Score persistence occurs in completion lifecycle; repository and lifecycle tests enforce idempotency and the historical cutoff. Production canary repeat reads retained one immutable assessment. | Proved for current legacy evidence score |
| Separate score, coverage, source, version, time, and finding lineage | `scan_score_assessments` stores separate typed fields; report, Pulse, dashboard, and admin projections retain metadata. The forward projection now uses canonical presentation status `surface`; the former `surfaced` typo could omit finding IDs from already-immutable provenance rows, which remain unmodified. | Proved forward; historical rows preserved rather than rewritten |
| Preserve historical meaning | Pre-cutoff records remain `Legacy scan score`; version identity is immutable by scan/kind/version. | Proved |
| Precisely label current headline | Current headline is `GDPR/ePrivacy evidence`, not overall CertScore. | Proved in current surfaces |
| Remove AI domain-name expected overall score | Domain benchmark prompt explicitly forbids estimating CertScore/overall score; repository search finds no expected-overall-score comparison. | Proved |
| Consume only canonical WC01 projections | Shadow input accepts typed GDPR/ePrivacy checklist rows and surfaced/reportable unified findings from the concern/policy pipeline; raw and diagnostic fields are absent. | Proved |
| Explicit complete registries and no fallback weights | Checklist registry equality, model audit, unknown-row rejection, stale/missing-family tests, and required weight lookup are present. | Proved |
| Separate risk from coverage | Candidate emits observed risk independently and withholds posture for inadequate coverage; comparison schema v5 separately names model eligibility and report usable evidence. Luna selected exact report usable evidence as the customer-facing metric and retained model eligibility coverage as an internal withholding input. | Proved and Luna-selected |
| Monotonicity and family deduplication | Deterministic scorer tests cover finding addition, severity ordering configuration, and sibling deduplication. | Proved at scorer level |
| Critical caps prevent misleadingly strong core-gap scores | Consent/tracking, contradiction, and sensitive-data caps work. Luna selected candidate-v3's rights-family maximum of 30, moving the deterministic high-severity rights gap to `70 / Watch / Review`. The benchmark contradiction is resolved, but retained replay still lacks a rights-gap input and the model remains pending. | Deterministic contradiction resolved; live corpus validation pending |
| Align score bands, posture, action, and top findings | Score/posture/action share one band table. Customer top-finding alignment cannot be verified until replacement integration exists. | Partial |
| Versioned bounded shadow comparisons | Schema v5 artifacts are bounded and versioned, carry projection fingerprints, exact normalized-request target keys, both coverage meanings, deltas, accepted migration differences, and unresolved contradictions. | Proved |
| Deterministic fixtures | Twelve required lanes are represented; structural, cross-region, and source-equivalence invariants run. Luna's expected bands are explicit and future mismatches are acceptance blockers. | Proved for deterministic candidate-v3 benchmark |
| Retained replay | 11/11 bundles projected; 7 scored, 4 withheld, 0 projection failures. All 11 retain the known legacy/report coverage divergence as a Luna-accepted migration difference; unresolved contradiction rate is now 0. | Proved for current retained corpus, not Luna-approved corpus |
| Owned canaries | Production org and anonymous canaries verified legacy persistence and Pulse/report consistency. The earlier four-row cross-region artifact remains withdrawn because hostname equality did not guarantee requested-URL equality. Revision `2b0ae2e0` fixed the canonical Umami collection endpoint (`gateway.umami.is/api/send`), made canonical vendor fetch/XHR/beacon activity project as collection evidence, and made Lambda score materialization fail closed when its retained bundle is unavailable. Retained Lambda scan `d54f0b91-3055-4525-93c1-981ddba8c54b` now reprojects to the same `preconsent_tracking` finding and `54 / Watch` score as browser-extension scan `90a12ebd-6672-4764-b9ae-95c37f530a02`. Fresh post-deploy Lambda scan `f1270be0-ba24-4ae7-a6ca-224af24ba296` independently reproduced the finding and score in 16 seconds. | Classifier mismatch fixed and live parity proved; formal equivalence gate remains open |
| Governed rotating public sample | Central export refreshed successfully at `2026-07-22T07:44:51Z`; selector failed closed with 0/10 eligible targets (47 cooldown, 1 blocked, 2 do-not-calibrate). Earliest current cooldown expiry is `2026-07-24T01:51:27Z`. | **Incomplete** |
| Luna corpus, bands, weights, caps, thresholds, monitoring baselines, and sign-off | Luna selected candidate-v3 and labeled all twelve expected bands. Decision schema v2 now requires bounded minimum sample/comparison counts and maximum drift, contradiction, withholding, region-range, and source-range limits with Luna evidence. The production monitor withholds alert judgment while those values remain pending and pauses rollout on undersampling or an exceeded approved limit. The machine-readable gate still requires the governed corpus, parameter approval, monitoring-baseline approval, and exact model-version sign-off. | **Partial; corpus, baselines, and final approval incomplete** |
| Accessibility, transport/security, and consumer protection | Deterministic cases prove these do not silently enter the GDPR/ePrivacy candidate and overall score remains withheld. They do not yet have approved domain-score contracts for an overall model. | Partial; overall appropriately withheld |
| Cross-region and Lambda/browser-extension equivalence | Deterministic reordered-input cases pass, and monitoring requires both hashed hostname and exact normalized-request identity. Live same-URL Lambda scans `f1270be0-ba24-4ae7-a6ca-224af24ba296` (`eu-central-1` / `eu_de`) and `c1f304d4-4f92-4a1a-bc0b-16a7906c6379` (`eu-west-1` / `eu_ie`) both project `preconsent_tracking` and candidate 54, a cross-region range of 0. The monitor now groups regional scan-from values under the `lambda` source family, rather than incorrectly treating `eu_de` and `eu_ie` as different scanner technologies. The browser-extension scan also agrees on the finding and score, but remains parity evidence rather than a formal source-equivalence cohort because its geography is unknown and its coverage fingerprint differs. | **Cross-region live gate passed; strict source-equivalence sample pending** |
| Registry, contradiction, historical, and Pulse/report regression gates | Focused tests and production canaries exist; contradiction monitoring is present in cohort summaries. | Partial; replacement surface tests remain impossible before integration |
| Public methodology | The deployed `/methodology` page states that a GDPR/ePrivacy score is a versioned domain assessment based on projected evidence/findings, presents coverage separately, permits withholding for insufficient evidence, and explicitly says it is not an overall CertScore, compliance determination, or certification. Candidate-specific weights and thresholds remain unpublished while unapproved. | Proved for current public semantics; candidate parameters appropriately pending |
| Controlled rollout flag and rollback | A single disabled-by-default selector now spans report, Pulse exports, dashboard/history, and admin. It requires exact Luna approval and exact kind/version, preserves candidate withholding, and fails closed to legacy. The rollback runbook restores legacy selection without rewriting history. Production revision `1fde59d3d7d2f048c296b96f6f2ea367d2412262` deployed this architecture with the selector env unset, which resolves to legacy mode. | Deployed disabled; Luna approval and enablement pending |
| Web, Pulse, dashboard, exports, and admin agreement | All consumers use the same configured persisted selection and preserve kind/version/coverage metadata. The production owned-canary verification retained the legacy evidence assessment and created no posture assessment while approval is pending. Candidate equivalence still requires production verification after final approval. | Deployed legacy path verified; final candidate equivalence pending |
| Production monitoring | Completion lifecycle persists immutable bounded shadow comparison metrics for drift, contradictions, withholding, and hashed comparison groups. Cross-region comparisons hold scanner source family constant; cross-source comparisons hold region constant; exact requested-URL keys prevent different paths on one hostname from being grouped. Regional Lambda values such as `eu_de` and `eu_ie` normalize to source family `lambda`, while extension sources remain separate. Production revision `2473cae1` was verified against six seven-day samples: five scored, one withheld, zero contradictions, and one valid two-region Lambda group for the same homepage with min 54, max 54, and range 0. Decision schema v2 now fail-closes alert evaluation until Luna approves every baseline; the null-region extension row remains deliberately excluded from the formal cross-source range. | Metrics deployed; machine-enforced baseline approval implemented and awaiting deployment/Luna values |
| Final approved replacement deployed and verified | Candidate-v3 is deployed for shadow measurement only and remains cutover-ineligible while its exact Luna packet is pending. No `gdpr_eprivacy_posture` row was created for the production canary. | **Incomplete** |

## Current hard gates

1. The canonical public selector produces at least 10 cooldown-eligible targets after a successful central-history export.
2. Capture a genuinely comparable Lambda/browser-extension source pair. The same-URL cross-region Lambda pair now has a candidate range of 0, and the Umami resolver and requested-URL identity fixes have live parity evidence. Browser geography must remain explicit/unknown rather than being inferred from a Lambda region.
3. Luna approves the benchmark corpus, parameters, and exact model version with attached evidence.
4. Luna approves the bounded production-monitoring baselines; alert evaluation must be within those baselines.
5. Pass `pnpm score:luna-cutover-gate`, enable the exact approved version, and verify all production consumers before claiming completion.
